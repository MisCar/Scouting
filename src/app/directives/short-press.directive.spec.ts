import { ShortPressDirective } from "./short-press.directive"

describe("ShortPressDirective", () => {
  it("should create an instance", () => {
    const directive = new ShortPressDirective()
    expect(directive).toBeTruthy()
  })
})
